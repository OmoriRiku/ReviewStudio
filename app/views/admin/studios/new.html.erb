<!--スタジオ新規登録フォーム-->
<div class="container">
  <div class="row">
    <div class="col-md-9 mx-auto">
    <h2 class="text-center">スタジオ新規登録</h2>
      <%= render 'layouts/errors', obj: @studio %>
      <div class="mt-5">
      <%= form_with model: @studio, url: studios_path, method: :post do |f| %>
        <%= render :partial => 'form', :locals => { :f => f } %>
      <% end %>
      </div>
    </div>
  </div>
</div>