<div class="container">
  <div class="row">
    <div class="col-md-3">
      <%= render 'public/uses/side_use', uses: @uses %>
      <%= render 'admin/stores/side_store', stores: @stores %>
    </div>
    <div class="col-md-9">
      <%= render 'index', word: @word, studios: @studios %>
    </div>
  </div>
</div>
